SELECT Name from ships;

SELECT Name from ships where CapitanID = 4;

SELECT Name, Surname, Age FROM passenger order by Age;

SELECT max(Age) FROM passenger GROUP BY ShipID;

SELECT DISTINCT(CapitanID) FROM Ships;

SELECT Name, PortID, RouteID FROM Ships;

SELECT avg(Age) FROM passenger WHERE ShipID = 1;

SELECT (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2);

